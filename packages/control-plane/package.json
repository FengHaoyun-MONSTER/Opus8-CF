{
  "name": "@opus8-cf/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --format=esm --external:cloudflare:sockets --outfile=dist/index.js",
    "test:integration": "node test/integration.mjs",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@opus8-cf/shared": "workspace:*"
  }
}
